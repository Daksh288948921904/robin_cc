# ============================================================
# robin cc — Hostinger VPS Production Requirements
# Python 3.11 | Ubuntu 22.04 | CPU-only
# ============================================================
# Install with:
#   pip install -r requirements-hostinger.txt
# ============================================================

# ── Web server ───────────────────────────────────────────────
flask==3.0.3
gunicorn==22.0.0

# ── Scraping ─────────────────────────────────────────────────
feedparser==6.0.10
newspaper3k==0.2.8
beautifulsoup4==4.12.2
lxml>=5.3.0
lxml_html_clean>=0.4.4
requests==2.31.0
httpx==0.28.1
tenacity==9.1.2
trafilatura==2.0.0

# ── NLP — similarity search (CPU-only torch, ~900 MB) ────────
# Pin to CPU-only torch to avoid downloading the 2 GB CUDA build
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu
sentence-transformers==3.0.1
transformers>=4.41.0,<4.47.0
tokenizers>=0.19.0
huggingface_hub>=0.20.0
scikit-learn>=1.3.0
numpy>=1.24.0,<2.0

# ── LLM (Groq — free, no GPU needed) ─────────────────────────
groq==1.0.0

# ── Image generation (HuggingFace API — no GPU needed) ───────
pillow==10.1.0

# ── Document export ───────────────────────────────────────────
python-docx==1.1.2

# ── Translation ───────────────────────────────────────────────
deep-translator==1.11.4
langdetect==1.0.9

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.0
pyyaml==6.0.1
python-dateutil==2.8.2
pytz==2023.3
validators==0.22.0
geopy==2.4.1
loguru==0.7.2
certifi>=2023.7.22
